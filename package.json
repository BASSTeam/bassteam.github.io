{
    "name": "github-pages-site",
    "scripts": {
        "start": "yarn build && serve",
        "build": "node --experimental-modules build.mjs",
        "postinstall": "git checkout node_modules/components && git checkout node_modules/containers"
    },
    "engines": {
        "node": ">=8.5.0"
    },
    "dependencies": {
        "mkdir-recursive": "^0.4.0",
        "serve": "^9.4.2",
        "uglify-es": "^3.3.9",
        "yarn": "^1.9.4"
    }
}
